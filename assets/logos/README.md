# Partner logos

Drop one SVG (or PNG fallback) per partner here. Files referenced by the README:

- `claude-code.svg`
- `codex.svg`
- `cursor.svg`
- `gemini.svg`
- `github.svg`
- `mcp.svg`

**Sourcing rule:** use each tool's official brand asset (anthropic.com brand kit,
cursor.com press, OpenAI brand assets, GitHub Octicons, etc.). Do NOT lift logos
from another open-source project's README — their license doesn't transfer to
the partner's mark.

Until these files land, the README's "Works with" table renders broken-image
icons with alt text. That's intentional: locks the slot, signals the TODO.
