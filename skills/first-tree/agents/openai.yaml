interface:
  display_name: "First Tree"
  short_description: "Top-level First Tree routing + pre-task hygiene"
  default_prompt: "Use $first-tree to give me the top-level First Tree map (workspace collaboration vs. Context management), run the mandatory pre-task hygiene checks (binding / tree HEAD freshness / role), and route me to the right sub-skill."
