interface:
  display_name: "Context Tree Review"
  short_description: "Review a GitHub PR or GitLab MR safely"
  default_prompt: "Use $context-tree-review for this trusted provider-scoped Context Reviewer run. Require the live binding and Reviewer assignment, review an exact detached head validator-first, repair every safely determined finding, then use the GitHub App verdict plus local exact-head merge or a GitLab note plus one local exact-SHA squash merge. Never simulate GitLab approval or retry an unknown merge."
