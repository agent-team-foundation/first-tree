interface:
  display_name: "Context Tree Review"
  short_description: "Review a Context Tree PR against generated policy"
  default_prompt: "Use $context-tree-review to inspect this trusted GitHub App Context Reviewer run at its immutable RUN_HEAD, repair only live changed non-protected files, publish one exact-head App review, and make one local exact-head squash-merge attempt only after approval."
