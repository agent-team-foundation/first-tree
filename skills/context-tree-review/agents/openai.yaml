interface:
  display_name: "Context Tree Review"
  short_description: "Review a GitHub Context Tree PR safely"
  default_prompt: "Use $context-tree-review to inspect the latest live state for this trusted GitHub App Context Reviewer run, make any appropriate repair with the local identity, publish the verdict through the App, and use one response-head REST squash-merge CAS plus read-only reconciliation through local gh only after approval; GitLab MRs use ordinary GitLab review."
