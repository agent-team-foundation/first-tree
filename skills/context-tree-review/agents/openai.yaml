interface:
  display_name: "Context Tree Review"
  short_description: "Review a GitHub Context Tree PR safely"
  default_prompt: "Use $context-tree-review to inspect the latest live state for this trusted GitHub App Context Reviewer run, repair every safely determined finding with the local identity before escalating only protected or blocked residuals, publish the verdict through the App, and squash-merge through local gh only after approval; GitLab MRs use ordinary GitLab review."
