interface:
  display_name: "First Tree Write"
  short_description: "Provider-aware source-driven Context Tree write"
  default_prompt: "Use $first-tree-write to reflect this source artifact into the Context Tree, rechecking the live provider/binding before mutation and leaving GitHub App or GitLab Webhook review dispatch to the server."
