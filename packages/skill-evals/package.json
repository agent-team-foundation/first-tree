{
  "name": "@first-tree/skill-evals",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "description": "Human-directed live model evaluations for First Tree skills.",
  "scripts": {
    "eval:floor": "tsx src/index.ts floor",
    "eval:gate": "tsx src/index.ts gate",
    "eval:periodic": "tsx src/index.ts periodic",
    "eval:quality": "tsx src/index.ts quality",
    "eval:select": "tsx src/index.ts select",
    "eval:summary": "tsx src/index.ts summary",
    "eval:compare": "tsx src/index.ts compare",
    "test": "vitest run --passWithNoTests --exclude '.runs/**'",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.16.0"
  },
  "dependencies": {
    "@first-tree/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.16.0",
    "tsx": "^4.21.0",
    "yaml": "^2.8.3"
  }
}
