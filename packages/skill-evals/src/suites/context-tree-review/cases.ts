import type { SkillEvalCase } from "../../core/case-schema.js";
import type { SkillEvalSuiteDefinition } from "../types.js";
import type { ContextTreeReviewEvalCase } from "./types.js";

const FLOOR_CASE_ID = "context-tree-review-static-coverage";
const prompt =
  "A trusted server-authored GitHub Context Reviewer wake-up is active in this eval runtime for pull request owner/context-tree#42 and run 01900000-0000-7000-8000-000000000042. Use context-tree-review to perform the review. This eval workspace contains the full default First Tree skill family, the bound Context Tree, and a deterministic local mirror whose origin exposes `refs/pull/42/head`. Keep the detached review worktree at `.review-worktrees/42`, run validation only with that worktree as the current directory, and make every semantic file read explicitly resolve through that registered worktree path. Submit the single correct outcome only through `first-tree tree review`.";

/**
 * Workflow scenarios pinned by the static floor and exercised across the live
 * gate plus the formal cross-surface QA case. Merge effects remain recorded
 * inside the deterministic GitHub shim; no real provider mutation occurs.
 */
export const CONTEXT_TREE_REVIEW_WORKFLOW_SCENARIOS = [
  "validator-failure",
  "semantic-failure",
  "passing",
  "merge-response-provenance",
  "merge-head-race",
  "merge-api-unsupported",
  "merge-queue-required",
  "merge-delivery-open",
  "merge-delivery-merged",
  "merge-delivery-unknown",
  "draft",
  "archive-only",
  "authority",
] as const;

export const CONTEXT_TREE_REVIEW_GATE_CASES: readonly ContextTreeReviewEvalCase[] = [
  {
    id: "validator-failure-requests-changes",
    fixture: { scenario: "validator-failure" },
    expected: {
      action: "request-changes",
      bodyHints: ["TREE_OWNERS_INVALID", "system/review-contract.md"],
      mergeOutcome: "not-attempted",
      verifyMustPass: false,
    },
    prompt,
    briefingMode: "minimal",
    provider: "codex",
    skill: "context-tree-review",
    status: "implemented",
    tags: ["validator-first"],
    tier: "gate",
  },
  {
    id: "semantic-failure-requests-changes",
    fixture: { scenario: "semantic-failure" },
    expected: {
      action: "request-changes",
      bodyHints: ["implementation", "source"],
      mergeOutcome: "not-attempted",
      verifyMustPass: true,
    },
    prompt,
    briefingMode: "minimal",
    provider: "codex",
    skill: "context-tree-review",
    status: "implemented",
    tags: ["semantic"],
    tier: "gate",
  },
  {
    id: "passing-ready-approves",
    fixture: { scenario: "passing" },
    expected: { action: "approve", bodyHints: [], mergeOutcome: "merged", verifyMustPass: true },
    prompt,
    briefingMode: "minimal",
    provider: "codex",
    skill: "context-tree-review",
    status: "implemented",
    tags: ["approve"],
    tier: "gate",
  },
  {
    id: "merge-head-comes-from-review-response",
    fixture: { scenario: "merge-response-provenance" },
    expected: { action: "approve", bodyHints: [], mergeOutcome: "merged", verifyMustPass: true },
    prompt,
    briefingMode: "minimal",
    provider: "codex",
    skill: "context-tree-review",
    status: "implemented",
    tags: ["approve", "merge-cas", "response-provenance"],
    tier: "gate",
  },
  {
    id: "approved-head-race-stays-open",
    fixture: { scenario: "merge-head-race" },
    expected: { action: "approve", bodyHints: [], mergeOutcome: "head-mismatch", verifyMustPass: true },
    prompt,
    briefingMode: "minimal",
    provider: "codex",
    skill: "context-tree-review",
    status: "implemented",
    tags: ["approve", "merge-cas", "head-race"],
    tier: "gate",
  },
  {
    id: "unsupported-merge-api-stays-open",
    fixture: { scenario: "merge-api-unsupported" },
    expected: { action: "approve", bodyHints: [], mergeOutcome: "api-unsupported", verifyMustPass: true },
    prompt,
    briefingMode: "minimal",
    provider: "codex",
    skill: "context-tree-review",
    status: "implemented",
    tags: ["approve", "merge-cas", "unsupported-gh"],
    tier: "gate",
  },
  {
    id: "merge-queue-requirement-stays-open",
    fixture: { scenario: "merge-queue-required" },
    expected: { action: "approve", bodyHints: [], mergeOutcome: "queue-required", verifyMustPass: true },
    prompt,
    briefingMode: "minimal",
    provider: "codex",
    skill: "context-tree-review",
    status: "implemented",
    tags: ["approve", "merge-cas", "merge-queue"],
    tier: "gate",
  },
  {
    id: "merge-transport-error-reconciles-open",
    fixture: { scenario: "merge-delivery-open" },
    expected: { action: "approve", bodyHints: [], mergeOutcome: "delivery-open", verifyMustPass: true },
    prompt,
    briefingMode: "minimal",
    provider: "codex",
    skill: "context-tree-review",
    status: "implemented",
    tags: ["approve", "merge-cas", "reconciliation"],
    tier: "gate",
  },
  {
    id: "merge-transport-error-reconciles-merged",
    fixture: { scenario: "merge-delivery-merged" },
    expected: { action: "approve", bodyHints: [], mergeOutcome: "delivery-merged", verifyMustPass: true },
    prompt,
    briefingMode: "minimal",
    provider: "codex",
    skill: "context-tree-review",
    status: "implemented",
    tags: ["approve", "merge-cas", "reconciliation"],
    tier: "gate",
  },
  {
    id: "merge-transport-error-remains-unknown",
    fixture: { scenario: "merge-delivery-unknown" },
    expected: { action: "approve", bodyHints: [], mergeOutcome: "delivery-unknown", verifyMustPass: true },
    prompt,
    briefingMode: "minimal",
    provider: "codex",
    skill: "context-tree-review",
    status: "implemented",
    tags: ["approve", "merge-cas", "reconciliation"],
    tier: "gate",
  },
  {
    id: "passing-draft-defers",
    fixture: { scenario: "draft" },
    expected: {
      action: "comment",
      bodyHints: ["draft", "ready"],
      firstHeading: "## Approval deferred",
      mergeOutcome: "not-attempted",
      verifyMustPass: true,
    },
    prompt,
    briefingMode: "minimal",
    provider: "codex",
    skill: "context-tree-review",
    status: "implemented",
    tags: ["draft"],
    tier: "gate",
  },
  {
    id: "archive-only-comments",
    fixture: { scenario: "archive-only" },
    expected: {
      action: "comment",
      bodyHints: ["archive/supporting", "out of scope"],
      mergeOutcome: "not-attempted",
      verifyMustPass: true,
    },
    prompt,
    briefingMode: "minimal",
    provider: "codex",
    skill: "context-tree-review",
    status: "implemented",
    tags: ["content-class"],
    tier: "gate",
  },
  {
    id: "authority-needs-human",
    fixture: { scenario: "authority" },
    expected: {
      action: "comment",
      bodyHints: ["authority", "owner"],
      firstHeading: "## Human decision required",
      mergeOutcome: "not-attempted",
      verifyMustPass: true,
    },
    prompt,
    briefingMode: "minimal",
    provider: "codex",
    skill: "context-tree-review",
    status: "implemented",
    tags: ["human-authority"],
    tier: "gate",
  },
];

export const CONTEXT_TREE_REVIEW_EVAL_CASES: readonly SkillEvalCase[] = [
  {
    briefingMode: "minimal",
    expected: { gateCaseIds: CONTEXT_TREE_REVIEW_GATE_CASES.map((item) => item.id) },
    fixture: { scenarios: CONTEXT_TREE_REVIEW_GATE_CASES.map((item) => item.fixture.scenario) },
    id: FLOOR_CASE_ID,
    skill: "context-tree-review",
    status: "implemented",
    tier: "floor",
  },
  ...CONTEXT_TREE_REVIEW_GATE_CASES,
];

export const CONTEXT_TREE_REVIEW_SUITE: SkillEvalSuiteDefinition = {
  cases: CONTEXT_TREE_REVIEW_EVAL_CASES,
  coverage: {
    skill: "context-tree-review",
    tiers: [
      {
        caseIds: [FLOOR_CASE_ID],
        description: "Validate review skill routing and gate fixture coverage.",
        status: "implemented",
        tier: "floor",
      },
      {
        caseIds: CONTEXT_TREE_REVIEW_GATE_CASES.map((item) => item.id),
        description: "Live validator-first and verdict-mapping review cases.",
        status: "implemented",
        tier: "gate",
      },
    ],
  },
  skill: "context-tree-review",
};
