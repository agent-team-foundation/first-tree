# Workspace Fixtures

Store reusable workspace fixtures here when they are intentionally maintained as QA assets.

Run-specific workspaces, generated context, and temporary source material belong in the temporary run directory.
