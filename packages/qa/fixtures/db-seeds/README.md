# Database Seed Fixtures

Store reusable, non-sensitive database seed fixtures here when they are useful across QA tasks.

Run-specific SQL output, imports, snapshots, and diffs belong in the temporary run directory.
