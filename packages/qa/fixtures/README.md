# QA Fixtures

This directory stores reusable QA assets. It does not store run output.

Per-run seed payloads, screenshots, logs, reports, bug artifacts, database dumps, and generated files belong in the
temporary run directory, not in this package.
