# Provider Binary Fixtures

Store reusable provider-binary fixtures here only when they are safe, deterministic, and intentionally part of QA asset
maintenance.

Generated or downloaded binaries for a specific QA run belong in that run's temporary directory.
