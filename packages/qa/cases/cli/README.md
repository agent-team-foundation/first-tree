# CLI Cases

Use this area for QA cases where the First Tree command surface, shell behavior, command output, local config handling,
or operator workflow is the primary validation target.

If the CLI is only a convenient entry point for validating another product behavior, place the case in the area that owns
that behavior and mention the CLI in the case body.
