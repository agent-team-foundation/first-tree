/**
 * Community / support destinations, shared by the onboarding finale
 * (CommunityBlock in step-start-chat.tsx) and the top-bar SupportMenu.
 *
 * PLACEHOLDERS: both values below ship as placeholders until the real
 * Discord invite and WeChat group QR are provided — swap them here (and the
 * image file under public/community/) in one place.
 */
export const DISCORD_INVITE_URL = "https://discord.gg/first-tree-placeholder";

/** Served from packages/web/public/. */
export const WECHAT_QR_SRC = "/community/wechat-qr.png";
