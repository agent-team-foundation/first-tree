fileType: momentic/test/v2
id: circling-deep-speed-returns-linearly
description: A brand-new user signs up and lands in onboarding
defaultEnv: local
steps:
  - module:
      path: ./modules/sign-up-fresh-user.module.yaml
  - assert: the user is signed in (a Sign out control is present) and onboarding
      step 1 of 3 asks the user to create a First Tree team, with a prefilled
      team name field and a Continue button
