services:
  server:
    build: .
    ports:
      - "${FIRST_TREE_HUB_PORT:-8000}:8000"
    environment:
      FIRST_TREE_HUB_DATABASE_URL: postgresql://${POSTGRES_USER:-firsttreehub}:${POSTGRES_PASSWORD:-firsttreehub}@postgres:5432/${POSTGRES_DB:-firsttreehub}
      FIRST_TREE_HUB_HOST: "0.0.0.0"
      FIRST_TREE_HUB_PORT: "8000"
      FIRST_TREE_HUB_JWT_SECRET: ${FIRST_TREE_HUB_JWT_SECRET}
      FIRST_TREE_HUB_ENCRYPTION_KEY: ${FIRST_TREE_HUB_ENCRYPTION_KEY}
      FIRST_TREE_HUB_CONTEXT_TREE_REPO: ${FIRST_TREE_HUB_CONTEXT_TREE_REPO}
      FIRST_TREE_HUB_GITHUB_TOKEN: ${FIRST_TREE_HUB_GITHUB_TOKEN}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8000/healthz"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-firsttreehub}
      POSTGRES_USER: ${POSTGRES_USER:-firsttreehub}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-firsttreehub}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-firsttreehub}"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

volumes:
  pgdata:
