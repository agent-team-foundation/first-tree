# Render Blueprint — https://render.com/docs/infrastructure-as-code
# Deploy: connect this repo in Render Dashboard → auto-detects render.yaml

databases:
  - name: first-tree-hub-db
    plan: starter
    databaseName: firsttreehub
    user: firsttreehub
    ipAllowList: []

services:
  - type: web
    name: first-tree-hub
    runtime: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    envVars:
      - key: FIRST_TREE_HUB_DATABASE_URL
        fromDatabase:
          name: first-tree-hub-db
          property: connectionString
      - key: FIRST_TREE_HUB_HOST
        value: "0.0.0.0"
      - key: FIRST_TREE_HUB_PORT
        value: "8000"
      - key: FIRST_TREE_HUB_CONTEXT_TREE_REPO
        sync: false
      - key: FIRST_TREE_HUB_GITHUB_TOKEN
        sync: false
      - key: FIRST_TREE_HUB_GITHUB_WEBHOOK_SECRET
        sync: false
      - key: FIRST_TREE_HUB_ADMIN_PASSWORD
        generateValue: true
      - key: FIRST_TREE_HUB_JWT_SECRET
        generateValue: true
      - key: FIRST_TREE_HUB_ENCRYPTION_KEY
        generateValue: true
